# MyOwnCat

A small implementation of the Linux `cat` command written in C.

## Current features

- Read files from command-line arguments
- Read multiple files
- `--help` argument
- Dynamic memory allocation
- Basic file handling with `fopen` and `fread`

## Build

```bash
gcc main.c -o main
